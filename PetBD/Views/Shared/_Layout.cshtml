@using Microsoft.AspNetCore.Http
@using PetBD.Helpers
@{
    var cart = Context.Session.Get<List<Item>>("cart");
    var c = 0;
    if(cart != null)
    {
        c = cart.Count();
    }
}
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <title>@ViewData["Title"] - PetBD</title>
        <!-- Favicon-->
        <link rel="icon" type="image/x-icon" href="~/assets/favicon.ico" />
        <!-- Bootstrap icons-->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />
        <!-- Core theme CSS (includes Bootstrap)-->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="~/css/styles.css" rel="stylesheet" />
    </head>
  <body>
        <!-- Navigation-->
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container px-4 px-lg-5">
                <a class="navbar-brand" asp-action="index" asp-controller="home">PetBD.</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                        <li class="nav-item"><a class="nav-link active" aria-current="page" asp-action="index" asp-controller="home">Home</a></li>
                        <li class="nav-item"><a class="nav-link" asp-action="about" asp-controller="home">About</a></li>
                        <li class="nav-item"><a class="nav-link" asp-action="index" asp-controller="categories">Category</a></li>
                        <li class="nav-item"><a class="nav-link" asp-action="index" asp-controller="products">Products</a></li>
                        <li class="nav-item"><a class="nav-link" asp-action="index" asp-controller="sliders">Sliders</a></li>
                       <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Shop</a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <li><a class="dropdown-item" href="#!">All Products</a></li>
                                <li><hr class="dropdown-divider" /></li>
                                <li><a class="dropdown-item" href="#!">Popular Items</a></li>
                                <li><a class="dropdown-item" href="#!">New Arrivals</a></li>
                            </ul>
                        </li>
                    </ul>
                   
                    <ul  class=" mb-2 mb-lg-0 ms-lg-4">
                         @*<li class="nav-item dropdown">
                            <a class="btn btn-outline-light btn-sm" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false"><i class="bi-cart-fill me-1"></i>
                            Cart
                            <span class="badge bg-light text-dark ms-1 rounded-pill">
                                @c
                                </span></a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                @if (Context.Session.Get<List<Item>>("cart") != null)
                                {
                                    foreach (var product in Context.Session.Get<List<Item>>("cart"))
                                    {
                                    <li><a class="dropdown-item" href="#!">@product.Product.Name <span class="badge bg-light text-dark ms-1 rounded-pill">@product.Quantity</span> </a> </li>
                                }
                                <li><hr class="dropdown-divider" /></li>
                                <li><a class="p-3 small" href="#!">Checkout</a></li>
                            }
                            else
                            {
                                <li><span class="p-3">Nothing Found!</span></li>
                            }
                        </ul>
                        </li>
                        *@
                        <li class="nav-item"><a class="btn btn-outline-light btn-sm" asp-action="index" asp-controller="cart"><i class="bi-cart-fill me-1"></i>
                            Cart
                            <span class="badge bg-light text-dark ms-1 rounded-pill">
                                @c
                                </span></a></li>
                    </ul>
                    <partial name="_LoginPartial.cshtml" />
                </div>
            </div>
        </nav>
    <div style="min-height:500px;">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>
        <!-- Footer-->
        <footer class="py-5 bg-dark">
            <div class="container"><p class="m-0 text-center text-white">Copyright &copy; Your Website 2022</p></div>
        </footer>
        <!-- Bootstrap core JS-->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
        <!-- Core theme JS-->
        <script src="~/js/scripts.js"></script>
            <script src="~/lib/jquery/dist/jquery.min.js"></script>
        <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
        <script src="https://use.fontawesome.com/c560c025cf.js"></script>
        <script src="~/js/site.js" asp-append-version="true"></script>
        @await RenderSectionAsync("Scripts", required: false)
    </body>
</html>
